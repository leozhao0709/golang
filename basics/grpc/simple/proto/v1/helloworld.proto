syntax = "proto3";

option go_package = "protogen/v1;pb"; // 'path/to/generate(fromRoot);package_name'
package pb;

service Greeter {
  rpc SayHello (HelloRequest) returns (HelloResponse);
}

message HelloRequest {
  string name = 1;
}

message HelloResponse {
  string message = 1;
}
